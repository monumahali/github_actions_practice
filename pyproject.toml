[project]
name = "github-actions-practice"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pytest>=9.0.3",
]


[tool.setuptools]
py-modules = ["app"]
